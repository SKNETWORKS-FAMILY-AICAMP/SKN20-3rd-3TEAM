# === 핵심 라이브러리 (의존성 최소화 버전) ===
# 환경 설정
python-dotenv>=1.0.0

# LangChain (필수만 선택)
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# 벡터 DB (ChromaDB가 numpy, pandas 등을 이미 포함)
chromadb>=0.5.0
langchain-chroma>=0.1.0

# 웹 검색
tavily-python>=0.3.0

# 웹 UI
streamlit>=1.28.0

# HTTP 요청
requests>=2.31.0
