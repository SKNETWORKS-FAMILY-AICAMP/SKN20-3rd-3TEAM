# RAG 시스템 아키텍처 가이드

## 📋 개요

이 RAG (Retrieval-Augmented Generation) 시스템은 **책임분리 원칙(Separation of Concerns, SoC)**에 따라 모듈식으로 설계되었습니다. 각 모듈은 명확한 책임을 가지며 독립적으로 운영되고 필요할 때 조합됩니다.

---

## 🏗️ 디렉토리 구조

```
src/
├── config/                  # 설정 관리 (환경변수, 모델 설정, API 키)
│   ├── __init__.py
│   └── settings.py
│
├── utils/                   # 유틸리티 함수 (로깅, 직렬화)
│   ├── __init__.py
│   ├── logger.py           # 로깅 유틸리티
│   └── serialization.py    # JSON 직렬화
│
├── core/                    # 핵심 기능 (벡터스토어, 임베딩)
│   ├── __init__.py
│   ├── embeddings.py       # 임베딩 모델 관리
│   └── retrieval.py        # 기본 검색기
│
├── retrievers/             # 다양한 검색 방식
│   ├── __init__.py
│   ├── base.py             # 검색기 기본 인터페이스
│   ├── internal.py         # 내부 문서 검색 (Chroma)
│   └── web.py              # 웹 검색 (Tavily)
│
├── llm/                     # LLM 통합
│   ├── __init__.py
│   └── client.py           # OpenAI 클라이언트
│
├── classifiers/            # 질문 분류
│   ├── __init__.py
│   ├── base.py             # 분류기 기본 인터페이스
│   └── question_classifier.py  # 질문 분류 구현
│
├── handlers/               # 유형별 질문 처리
│   ├── __init__.py
│   ├── base.py             # 핸들러 기본 인터페이스
│   ├── medical.py          # 의료 질문 처리
│   ├── hospital.py         # 병원/지도 질문 처리
│   └── general.py          # 일반 질문 처리
│
├── external/               # 외부 API 통합
│   ├── __init__.py
│   └── kakao_map.py        # 카카오맵 API
│
├── pipelines/              # 파이프라인 오케스트레이션
│   ├── __init__.py
│   ├── base.py             # 파이프라인 기본 인터페이스
│   └── orchestrator.py     # 메인 오케스트레이터
│
├── data/                   # 데이터 처리
│   ├── __init__.py
│   ├── ingestion.py        # 데이터 수집
│   └── chunking.py         # 문서 청킹
│
└── __init__.py             # 패키지 초기화
```

---

## 🎯 각 모듈의 책임

### 1. **config/** - 설정 관리
- **책임**: 환경변수 관리, 모델 설정, API 키 통합
- **주요 특징**:
  - 싱글톤 패턴으로 전역 설정 제공
  - 타입 안전한 dataclass 기반 설정
  - 환경변수 자동 로드
- **사용 예**:
```python
from src.config import get_settings
settings = get_settings()
print(settings.llm.model)  # 모델명 조회
```

### 2. **utils/** - 유틸리티
- **책담**: 공통 기능 제공 (로깅, 직렬화)
- **주요 클래스/함수**:
  - `get_logger()`: 통일된 로깅
  - `serialize_result()`: JSON 직렬화
- **사용 예**:
```python
from src.utils import get_logger
logger = get_logger(__name__)
logger.info("정보 메시지")
```

### 3. **core/** - 핵심 기능
- **책임**: 벡터스토어, 임베딩, 기본 검색 관리
- **주요 함수**:
  - `get_embedding_model()`: 임베딩 모델 생성
  - `create_vectorstore()`: 벡터스토어 생성
  - `load_vectorstore()`: 벡터스토어 로드
  - `SimpleRetriever`: 기본 검색기
- **특징**: LangChain 호환성 보장

### 4. **retrievers/** - 검색 추상화
- **책임**: 다양한 검색 방식 제공
- **구현체**:
  - `InternalSearcher`: Chroma 벡터스토어 검색
  - `WebSearcher`: Tavily 웹 검색
- **인터페이스**: `BaseSearcher.search()`
- **사용 예**:
```python
from src.retrievers import InternalSearcher
searcher = InternalSearcher(vectorstore, top_k=5)
results = searcher.search("질문")
```

### 5. **llm/** - LLM 통합
- **책임**: 언어 모델 호출 추상화
- **주요 클래스**:
  - `LLMClient`: OpenAI API 래퍼
  - `get_llm_client()`: 싱글톤 인스턴스 제공
- **주요 메서드**:
  - `invoke()`: LLM 호출
  - `parse_json()`: JSON 응답 파싱
- **사용 예**:
```python
from src.llm import get_llm_client
llm = get_llm_client()
response = llm.invoke("안녕하세요")
```

### 6. **classifiers/** - 질문 분류
- **책임**: 질문을 의료(A) / 병원(B) / 일반(C)으로 분류
- **분류 과정**:
  1. 키워드 기반 분류 (빠름)
  2. LLM 기반 분류 (정확함)
- **주요 클래스**:
  - `QuestionClassifier`: 분류 구현
  - `QuestionType`: 분류 유형 열거형
- **사용 예**:
```python
from src.classifiers import QuestionClassifier
classifier = QuestionClassifier()
question_type, confidence, reason = classifier.classify("강아지 피부염 증상은?")
```

### 7. **handlers/** - 유형별 처리
- **책임**: 각 질문 유형에 맞는 처리 수행
- **구현체**:
  - `MedicalHandler`: 의료 질문 → RAG 기반 답변
  - `HospitalHandler`: 병원 질문 → JSON 조회
  - `GeneralHandler`: 일반 질문 → LLM 답변
- **인터페이스**: `BaseHandler.handle(query)`
- **특징**: 각 핸들러는 독립적으로 동작 가능

### 8. **external/** - 외부 API
- **책임**: 외부 서비스 통합
- **현재 구현**:
  - `HospitalMapper`: 카카오맵 API 통합
- **확장 가능**: 새로운 외부 API 쉽게 추가

### 9. **pipelines/** - 오케스트레이션
- **책임**: 전체 흐름 조정
- **주요 클래스**:
  - `RAGOrchestrator`: 분류 → 핸들러 라우팅 → 포맷팅
- **주요 메서드**:
  - `process(query)`: 단일 질문 처리
  - `batch_process(queries)`: 배치 처리
  - `interactive_mode()`: 대화형 모드
  - `save_results(results, path)`: 결과 저장

### 10. **data/** - 데이터 처리
- **책임**: 데이터 수집 및 전처리
- **주요 함수**:
  - `ingest_data()`: JSON 파일 로드
  - `chunk_documents()`: 문서 청킹
  - `chunk_documents_with_token_range()`: 토큰 범위 기반 청킹
- **특징**: 메타데이터 자동 관리

---

## 🔄 데이터 흐름

### 일반적인 질문 처리 흐름

```
사용자 입력
    ↓
[Orchestrator] process(query)
    ↓
[QuestionClassifier] classify(query)
    ├─→ 키워드 기반 분류
    └─→ LLM 기반 분류 (필요시)
    ↓
질문 유형 판단
    ├─ MEDICAL (A) ──→ [MedicalHandler]
    │                     ├─ [InternalSearcher] (Chroma)
    │                     ├─ [LLMClient] (평가)
    │                     ├─ [WebSearcher] (필요시)
    │                     └─ 답변 생성
    │
    ├─ HOSPITAL (B) ──→ [HospitalHandler]
    │                     ├─ JSON 데이터 조회
    │                     ├─ 위치 기반 필터링
    │                     └─ 결과 생성
    │
    └─ GENERAL (C) ──→ [GeneralHandler]
                         ├─ [LLMClient]
                         └─ 답변 생성
    ↓
결과 포맷팅 + 메타데이터 추가
    ↓
사용자에게 반환
```

### 의료 질문 상세 처리

```
의료 질문 입력
    ↓
[MedicalHandler.handle()]
    ↓
1️⃣ 내부 문서 검색
    ├─ [InternalSearcher.search()]
    ├─ Chroma 벡터스토어 조회
    └─ top_k 문서 반환
    ↓
2️⃣ 근거 평가
    ├─ [LLMClient] 관련성 평가
    ├─ 점수 계산
    └─ threshold와 비교
    ↓
3️⃣ 임계값 판단
    ├─ 내용 충분 → 답변 생성
    └─ 내용 부족 → 웹 검색
    ↓
4️⃣ 웹 검색 (필요시)
    ├─ [WebSearcher.search()]
    ├─ Tavily API 호출
    └─ 결과 통합
    ↓
5️⃣ 최종 답변 생성
    ├─ RAG 컨텍스트 구성
    ├─ [LLMClient.invoke()]
    └─ 포맷된 답변 반환
```

---

## 💡 사용 예시

### 1. 기본 사용법

```python
from src import RAGOrchestrator, get_embedding_model, load_vectorstore

# 벡터스토어 로드
embedding_model = get_embedding_model("openai")
vectorstore = load_vectorstore(embedding_model)

# 오케스트레이터 생성
orchestrator = RAGOrchestrator(
    vectorstore=vectorstore,
    llm_model="gpt-4o-mini",
    score_threshold=0.6
)

# 단일 질문 처리
result = orchestrator.process("강아지 피부염 증상은?")
print(result['formatted_answer'])
```

### 2. 배치 처리

```python
questions = [
    "강아지 피부염 증상은?",
    "강남역 근처 동물병원은?",
    "반려견 훈련 팁",
]

results = orchestrator.batch_process(questions)
orchestrator.save_results(results, "results.json")
```

### 3. 대화형 모드

```python
orchestrator.interactive_mode()
```

### 4. 개별 모듈 사용

```python
from src.retrievers import InternalSearcher
from src.llm import get_llm_client

# 검색만 수행
searcher = InternalSearcher(vectorstore, top_k=5)
results = searcher.search("피부염")

# LLM 호출만 수행
llm = get_llm_client()
answer = llm.invoke("강아지 피부염에 대해 설명해주세요")
```

---

## 🔧 확장 가능성

### 새로운 질문 유형 추가

1. `classifiers/question_classifier.py`에 키워드 추가
2. `handlers/` 디렉토리에 새로운 핸들러 클래스 생성
3. `RAGOrchestrator.process()`에 라우팅 로직 추가

### 새로운 검색기 추가

1. `retrievers/base.py`의 `BaseSearcher` 상속
2. `search()` 메서드 구현
3. `retrievers/__init__.py`에 등록

### 새로운 외부 API 추가

1. `external/` 디렉토리에 새로운 모듈 생성
2. 관련 API 클래스 구현
3. 핸들러나 파이프라인에 통합

---

## 📊 의존성 관계

```
┌─────────────────────────────────────┐
│         Config (settings)           │
└────────────────────┬────────────────┘
                     ↓
         ┌───────────────────────┐
         │   Utils (logging)     │
         └───────┬───────────────┘
                 ↓
    ┌────────────────────────────┐
    │   Core (embeddings, etc)   │
    └────┬───────────────┬───────┘
         ↓               ↓
    [Retrievers]    [LLMClient]
         ↑               ↑
         └───────┬───────┘
                 ↓
         [Classifiers]
                 ↓
         [Handlers] ← [External APIs]
                 ↓
         [Orchestrator]
```

---

## 🎓 설계 원칙

### 1. **단일 책임 원칙 (SRP)**
- 각 모듈은 하나의 명확한 책임만 가짐
- 예: `retrievers/`는 검색만, `llm/`은 LLM 호출만

### 2. **개방-폐쇄 원칙 (OCP)**
- 확장에는 열려있고 수정에는 닫혀있음
- Base 인터페이스를 통해 새로운 구현 추가 가능

### 3. **의존성 역전 원칙 (DIP)**
- 고수준 모듈이 저수준 모듈에 의존하지 않음
- 추상 인터페이스를 통한 느슨한 결합

### 4. **DRY (Don't Repeat Yourself)**
- 공통 기능은 `utils/`에 집중
- 반복되는 패턴은 베이스 클래스로 추상화

---

## 📝 향후 개선 사항

- [ ] 비동기 처리 지원
- [ ] 캐싱 메커니즘
- [ ] 결과 평가 모듈
- [ ] 사용자 피드백 수집
- [ ] 성능 모니터링
- [ ] 더 많은 외부 API 통합
- [ ] 멀티언어 지원

---

## 🤝 기여 가이드

새로운 기능을 추가할 때:

1. 적절한 모듈 선택 (또는 새 모듈 생성)
2. Base 인터페이스 확인
3. 로깅 추가
4. 설정 필요시 `config/settings.py` 수정
5. `__init__.py`에 export 추가
6. 문서화

---

**마지막 수정**: 2024년
**버전**: 1.0.0

